# Session Summary — 2026-07-05

> **📌 Current progress snapshot: [summary.md](summary.md)** — what's done, what's pending verification, and what's left to build. Read that first when resuming work (e.g. after `/clear`); this file is the decision history.

## What this session was about
Revised [docs/SPEC.md](../docs/SPEC.md) (the functionality spec for the Market-Side crypto/NGN trading platform) to reflect a new set of decisions. No source code exists in this repo yet — this was a documentation-only session.

## Decisions made

1. **OTP → captcha.** All SMS/OTP verification is removed (no Termii/Twilio, no SMS provider at all). Replaced with **svg-captcha** generated server-side behind a `CaptchaService`, required on **registration, login, and password reset**. Challenges are id'd, answer stored hashed with short TTL, single-use, attempt-limited.
2. **Terms checkbox is frontend-only.** The backend no longer stores or enforces `termsAccepted`; the register form just disables submit until the box is checked. Backend storage can be added later if legal requires proof of acceptance.
3. **bcryptjs for hashing** (replacing argon2) — used for passwords, security-question answers, and the withdrawal PIN.
4. **Password reset via security question.** Users set a security question + answer at registration (answer normalized then hashed). Forgot-password flow: captcha → correct answer → set new password. Question changeable when authenticated with password re-entry.
5. **Withdrawal PIN set/change** (previously OTP-protected): now requires **TOTP if 2FA is enabled, otherwise password re-entry**.
6. **2FA = Google Authenticator** (TOTP; any compatible app).
7. **Referral QR code.** Referral link is also exposed as a QR code (server-generated with the `qrcode` package, data-URL/PNG endpoint) for the Team screen to render/download/share.
8. **In-app notifications** (new §2.9) for users *and* admins, pushed over Socket.IO — plus an admin-visible, filterable **audit log** feed so admins can see everything happening on the platform.
9. **NEW feature — Trading Signals** (new §2.7, not in project.txt):
   - Admin publishes fixed-return "contract order" signals: pair, direction, return %, min/max stake, duration, release date.
   - Released **daily between 3:00 pm and 5:00 pm Africa/Lagos** by a scheduled job.
   - **Signals cannot be repeated** — one join per user per signal (unique `user + signal` index).
   - Join stakes NGN via a held ledger entry; a settlement job credits stake + fixed return after the duration.
   - Maps to the "Contract order" item in the Mine section.
10. **Login alerts** go via in-app notification + email (no SMS).

## Changes applied to docs/SPEC.md

- §1 constraints table: frontend-only terms row, svg-captcha row, in-app+email notifications row, Africa/Lagos timezone row.
- §2.1 rewritten around captcha, bcryptjs, security question, Google Authenticator, new PIN-change rule.
- Inserted §2.7 Trading Signals and §2.9 In-App Notifications; later sections renumbered (Referral 2.8, Announcements 2.10, Admin 2.11, Security 2.12, Structure 2.13).
- Admin API gained: manage signals, view audit log, admin notification feed, signal-payout reports.
- Server structure updated: `CaptchaService`, `SignalService`, `NotificationService` (dropped `OtpService`); models add `Signal`, `SignalPosition`, `Notification`; jobs add signal release + settlement sweeps.
- Frontend screens: OTP Verify screen removed; Register/Login get captcha; new Forgot Password, Signals, and Notifications rows; Team gets the QR code.
- Recommendations + stack pins updated (bcryptjs, svg-captcha, qrcode; note that security-question resets are weaker than email resets — hash + rate-limit).
- Coverage map updated, with two new rows for Trading Signals and In-App Notifications as requirements not present in project.txt.

## Verified
Grep over the final spec confirms no stale `argon2`, `Termii`, `Twilio`, or backend `termsAccepted` references; the only "OTP"/"SMS" hits are "TOTP" (2FA) and lines explicitly stating OTP/SMS was replaced.

## Repo state / next steps
- Repo contains only `docs/SPEC.md`, `project.txt`, and `.claude/` — nothing committed yet, no code scaffolding.
- Natural next step: scaffold the Express server per §2.13 (config, models, routes, controllers, services, middleware, jobs) and start with auth + captcha.

## Client clarifications — 2026-07-05 (evening)

11. **Signals are binary options, not guaranteed returns** (corrects decision 9). Client's real signal format: pair + direction **CALL/PUT** + time limit in **seconds** (e.g. 60s) + a per-signal **trading window** (e.g. 18:00–20:00) distinct from the 3–5 pm release window. The user stakes NGN and picks a direction; entry price is snapshotted at entry, settlement price at entry+duration. Correct direction → **win** (stake + fixed return %); wrong → **lose the full stake**. **No tie case** — unchanged price counts as a loss. SPEC §2.7 rewritten; `Signal` (durationSeconds, tradingStart/End, call/put) and `SignalPosition` (direction, entryPrice, settlePrice, outcome, payout) updated; `SIGNAL_OUTCOMES` added to constants.
12. **Signal pairs are quoted vs NGN** (e.g. **BCH/NGN**) and the signal universe includes **BCH** beyond the four trading assets; USDT-quoted pairs may come later. `SIGNAL_ASSETS`/`SIGNAL_PAIRS` added; PriceService now quotes BCH/NGN (CoinGecko id `bitcoin-cash`), verified live.
13. **Crypto withdrawals**: sending real on-chain crypto requires either a custody/gateway API or self-managed hot wallets — there is no third option. Plan: launch with manual admin payout through the existing withdrawal-approval escrow flow; integrate a crypto payout provider later if volume justifies it.

## Client clarification — 2026-07-06

14. **The platform is dollar-denominated.** Deposits and withdrawals move in **Naira** (bank rail), but the user's money is held and displayed in **dollars**: on deposit confirmation NGN auto-converts to the USD balance (stored as micro-USDT in the USDT wallet) at the live USDT/NGN rate ± configurable spread; withdrawal converts USD→NGN at the withdrawal-day rate ± spread before payout. All internal denominations (signal stakes/payouts, referral commissions, adjustments, P/L) are dollars — `PLATFORM_CURRENCY = 'USDT'` in constants. Referral service switched from NGN to platform-currency payouts; Signal/SignalPosition stakes re-denominated; SPEC §2.2/§2.3-ish/§2.7/§2.8 updated. The NGN wallet becomes a gateway pass-through (deposit + conversion entries share one ledger group so it nets to zero).

## Payment gateway integration — 2026-07-06

15. **Gateway = Beidou** (MD5-signed "四方"-style, same merchant account as the client's sister project). Client shared their working helper; ours mirrors it in `payment.service.js` (ESM, fetch, no streamifier/axios deps). Key mechanics: ASCII-sorted `k=v&…&<secret>` MD5 sign; callbacks verified against the **raw request body** (`req.rawBody` captured in app.js) because `JSON.parse` destroys `"transAmt": 100.00`; callbacks answered with literal text `success` (else 8 retries); Nigeria payouts must be whole naira; callback amount is authoritative. **`callbackUrl` is passed per order** so this project's callbacks never hit the sister project's server; order IDs are `MS`-prefixed to keep the shared merchantOrderId namespace collision-free.
16. **Deposit flow**: intent locks the USD/NGN rate (live USDT/NGN ± admin spread, or admin fixed rate) → whole-naira collection order → hosted checkout. Completed callback credits the ACTUAL paid NGN and auto-converts at the locked rate in one ledger group (NGN nets zero), pays referral commissions on the USD amount, idempotent under retries.
17. **Withdrawal flow**: auto-submit like the sister project (not queue-first): PIN + TOTP-if-2FA → admin-configured Lagos-timezone days/hours window + daily limit + tiered fee (below/above threshold) → gross $ held via ledger → payout submitted; gateway failure refunds instantly; callback settles (paid) or releases (rejected) the hold. Admin manual approve/reject retained for reconciliation.
18. **Admin platform settings** (`GET|PUT /api/admin/settings`, Setting-persisted): min deposit/withdrawal, fee tiers, withdrawal window, daily limit, fx_mode/spreads. Defaults mirror the sister project (min $11.5, fees 16%/10% @ $100 threshold, window 08:00–22:00 daily).
19. **Improvements over the sister project**: ledger double-entry instead of direct balance mutation; escrow holds instead of upfront deduction; rate locked per order; Lagos wall-clock window (sister used server time); constant-time signature compare; settings persisted + audited.

## Admin functionality expansion + Spin & Win + testing — 2026-07-07

20. **Admin impersonation ("login as user")** — mirrored a support tool the client uses, re-tailored to this stack. `POST /api/admin/users/:id/impersonate` signs a 2h jose access token with `sub=target` + `imp=adminId` and **overwrites only the access cookie** (unlike the client's snippet, the admin's rotating refresh session is left intact, so exit/refresh restores the admin instead of stranding them). `POST /api/admin/impersonation/exit` is mounted **before** the role gate (the `imp` claim authorizes it). Staff targets are superadmin-only; while impersonating, `requireRole` 403s every admin route (`IMPERSONATION_ACTIVE`) and `GET /api/auth/me` exposes `impersonation.adminId`. Start + exit audited. Wallet credit/debit already existed as an audited `admin_adjustment` ledger entry (kept ledger-first, unlike the client's direct-balance snippet) — response enriched with balanceBefore/After.
21. **Admin reports** (`report.service.js`) — the last open §2.11 item: `GET /api/admin/reports/overview` (dashboard cards incl. signal house net + per-level referral payouts) and `/reports/timeseries?metric=…` (daily Lagos-calendar buckets). Decimal128 smallest-unit aggregations → display strings.
22. **NEW feature — Spin & Win** (client 2026-07-06). Circular wheel, 9 admin-configurable prizes (`spin_prizes` in settings). **Players only ever win the two lowest values**: lowest every spin, second-lowest on each Nth (`spin_bonus_every`, default 5) spin of the Lagos day **platform-wide** — driven by an atomic per-day `SpinCounter` + modulo (auto-resets every cycle and every day). Spin credits earned on **direct L1 referral registration** (`spin_referral_reward`, hook in `auth.service.register`) or admin grant; consumed by atomic conditional `$inc` with refund-on-failure. Prizes pay via ledger `spin_reward` credit + `Spin` row (carries `prizeIndex` = wheel segment for the frontend animation). New models `Spin`/`SpinCounter` (→ 17 total); `spinCredits` on User; endpoints `GET/POST /api/spin`, `GET /api/spin/history`, admin `GET /api/admin/spins` + grant.
23. **Security questions are now predefined** (client 2026-07-06, corrects decision 4). Users no longer invent their own — 10 fixed questions with **stable slug ids** in `src/config/securityQuestions.js`, served at `GET /api/auth/security-questions` for the frontend dropdown. Register takes `securityQuestionId`, change takes `questionId`; ids stable (not runtime-generated) since they persist on user docs.
24. **Testing framework = Vitest** (chosen over `node:test` so the React frontend repo shares one framework). Unit layer `tests/*.test.js` (money/time/phone/hash/pagination/security-questions/spin-rules, no DB) via `npm test`; the existing live-Atlas scripts stay as the integration layer; `npm run test:all` chains unit → smoke → e2e. §2.12 also corrected `validatejs` → **zod** (the actual implementation, long-standing deviation).
25. **Frontend connection** (both repos): backend already CORS-locks to `CLIENT_ORIGIN` with `credentials:true`; frontend uses relative `/api` + `/socket.io` proxied by a `vercel.json` rewrite in prod (dev uses the Vite proxy), `credentials:'include'`, echoes the `ms_csrf` cookie as `x-csrf-token` on mutations, refreshes on 401. No code-level link — the contract is docs/SPEC.md + the route list in summary.md.

## Security questions → recovery codes + sessions/devices — 2026-07-08

26. **Security questions removed → recovery codes** (client 2026-07-08). Security questions are weak (guessable; SPEC §5 already flagged them). Replaced with **10 one-time recovery codes** generated at registration (returned once, stored as sha256 hashes since codes are high-entropy — bcrypt's slowness is pointless here). Reset = captcha + one unused code (case/format-insensitive, single-use, revokes all sessions, uniform `RESET_FAILED`). `POST /api/auth/recovery-codes/regenerate` (password re-entry) reissues. Chose recovery codes over email reset because no email provider is wired yet — self-contained, always-available. Email reset can be added later as a convenience. Removed `config/securityQuestions.js`, its unit test, `User.security`, and the `/security-questions`, `/security-question`, `/security-question/change` endpoints; admin bootstrap no longer sets a security question. `utils/recoveryCodes.js` + `tests/recoveryCodes.test.js` added. **Frontend contract changed** (separate repo): register response now returns `recoveryCodes` (show once); Forgot Password takes `recoveryCode` not a security answer; the security-question dropdown + `GET /security-questions` are gone.
27. **Sessions & devices** (client 2026-07-08). `GET /api/sessions` (active logins with a parsed device label via `utils/userAgent.js`, IP, timestamps, `current` flag matched by hashing the ms_refresh cookie), `DELETE /api/sessions/:id` (log out that device), `POST /api/sessions/revoke-others` (log out other devices). Built over the existing `Session` model — no schema change; `session.service.js`. Revoking kills the refresh token immediately; the device's short-lived access token lapses within ACCESS_TOKEN_TTL.

## Trading signals reshaped — 2026-07-09

29. **Signals became admin-decided + window simplified** (client 2026-07-09, corrects the binary-options-by-price model from decisions 9/11). Four changes:
    - **Outcome is admin-decided, not price-based.** Each signal's `direction` (CALL/PUT) is the **secret winning side**; a contract wins iff the user's own pick matches it (`won = position.direction === signal.direction`), paying stake + `returnPct`%. Real market price is no longer what decides — entry/settle prices are still snapshotted (best-effort, cosmetic) for display continuity. Fixes "I followed the shown direction and lost": the shown badge was just a prediction.
    - **Admin direction is hidden from users.** `toDisplaySignal` takes `{ includeDirection }` — admin listings/CRUD include it, user-facing `listActive`/positions omit it (`toDisplayPosition` drops the nested `signal.direction`; the user's own pick is still shown). Frontend: removed the direction badge from user signal cards + the "follow the tip" preselect; the user picks CALL/PUT blind.
    - **Release window is admin-configurable + single-window model.** Dropped the per-signal trading window (`tradingStart/tradingEnd` removed from the Signal model, routes, and forms). New settings `signal_release_start`/`signal_release_end` (default 15:00–17:00 Lagos). A signal **created inside the window auto-releases immediately** and is tradeable; `placeOrder` accepts contracts only while a released signal is still inside the window (`tradeable` flag replaces `inTradingWindow`). Force-release still publishes early; being released-in-window is what makes it tradeable.
    - **Delete signal:** `DELETE /api/admin/signals/:id` hard-deletes a signal with **no contracts** on it (refuses with `SIGNAL_HAS_POSITIONS` otherwise — use cancel to refund; settled signals immutable). Admin UI got a Delete button + `api.del` helper.
    - Tests: `services-e2e` signal block rewritten (save/restore the release window, assert auto-release + hidden direction + deterministic matching-pick win). `SIGNAL_WINDOW`/`isWithinSignalWindow` in constants/time are now dead (superseded by settings).

## Multi-account switching — 2026-07-09

28. **Multi-account switching** (client 2026-07-09, reverses decision 27's "deferred to v2"). Gmail/Slack-style: multiple **full, independent** accounts signed into one browser, switch instantly. Confirmed decisions: (a) each is a separate registration (own phone/email/KYC/wallets/tree) — no data-model change; (b) adding requires a **full login** (captcha + password, TOTP if 2FA); (c) **block cross-benefit** between linked accounts; (d) cap **5** per browser; (e) default logout signs out only the active account (promotes next), plus a "log out others, keep active" control.
    - **httpOnly-safe design:** the active account uses `ms_access/ms_refresh/ms_csrf` exactly as before (all existing routes/refresh/CSRF/impersonation untouched). New **signed httpOnly `ms_accounts`** cookie (jose HMAC, `MULTI_ACCOUNT_SECRET`, prod refuses the dev default) holds only the *inactive* accounts `{id, rt, label}` + active pointer. Switching is server-mediated (rotate the target's session, swap active cookies) — the frontend never sees a token. `multiAccount.service.js`; endpoints under `/api/auth/accounts` (list/add/switch/remove/logout-others) + `auth.controller.logout` promotion.
    - **Durable anti-abuse:** accounts added together are unioned into `User.linkGroupId` (union-find; persists even after unlinking). `referral.payCommissions` + `spin.awardReferralSpin` skip beneficiaries sharing the source's link group. The one gap accepted: the registration-time referral **spin credit** fires before the link exists, so it can't be pre-blocked — the material ongoing referral **commissions** are fully blocked; links are audited (`account.link`).
    - Added `MAX_LINKED_ACCOUNTS`, `COOKIES.accounts`, `accountsCookieOptions`, `User.linkGroupId` (+index). `npm run test:http` extended (list → add → link-group formed → switch → logout-others → NOT_LINKED). **Frontend (separate repo):** switcher calls `GET /accounts`; switch → invalidate all caches; "Add account" reuses the login form → `POST /accounts/add`.
